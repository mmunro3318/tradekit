"""tradekit — deterministic core, thin LLM shell.

Architecture: docs/DESIGN.md. Public surfaces are each subpackage's
``__init__``; underscore-prefixed internals are private (DESIGN §1).
"""

__version__ = "0.1.0"
